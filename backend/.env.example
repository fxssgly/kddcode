# MySQL 用户名。
DB_USER=root
# MySQL 密码；本地无密码时可以留空。
DB_PASSWORD=
# MySQL 连接地址，默认连接本机 bigdata 数据库。
DB_URL=jdbc:mysql://127.0.0.1:3306/bigdata?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
# 是否优先从 MySQL 读取演示数据；false 时使用 CSV。
USE_MYSQL=true
# Python 3 命令或可执行文件路径。
KDD_PYTHON=python
